# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
        env:
          VITE_FIREBASE_API_KEY: "AIzaSyC-8rr-j_Q9c-EcK6MvJ0lwM4C9Y3zX0u0"
          VITE_FIREBASE_AUTH_DOMAIN: "beaditz.firebaseapp.com"
          VITE_FIREBASE_PROJECT_ID: "beaditz"
          VITE_FIREBASE_STORAGE_BUCKET: "beaditz.firebasestorage.app"
          VITE_FIREBASE_MESSAGING_SENDER_ID: "242942576100"
          VITE_FIREBASE_APP_ID: "1:242942576100:web:49a99041f491efa01433f1"
          VITE_RESEND_API_KEY: ${{ secrets.VITE_RESEND_API_KEY }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BEADITZ }}
          projectId: beaditz
